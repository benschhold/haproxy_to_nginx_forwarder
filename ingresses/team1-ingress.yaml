apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: team1-ingress
  namespace: haproxy-ingress
  annotations:
    kubernetes.io/ingress.class: haproxy
spec:
  rules:
  - host: team1.138.124.209.74.sslip.io
    http:
      paths:
      - pathType: Prefix
        path: /
        backend:
          service:
            name: team1-ingress-ingress-nginx-controller
            namespace: team1
            port:
              number: 443
